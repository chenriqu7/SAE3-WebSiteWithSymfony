    <div id="modal" class="absolute p-4 w-full place-self-center max-w-md max-h-full z-50 hidden">
        <!-- Modal content -->
        <div class="bg-white rounded-lg shadow dark:bg-gray-700">

            <!-- Modal header -->
            <div class="flex items-center justify-between p-4 md:p-5 border-b rounded-t dark:border-gray-600">
                <h3 class="text-lg font-semibold text-gray-900 dark:text-white" id="modal-title">
                    Ajout d'un nouveau SA
                </h3>
                <button class="text-gray-400 bg-transparent hover:bg-gray-200 hover:text-gray-900 rounded-lg text-sm w-8 h-8 ms-auto inline-flex justify-center items-center dark:hover:bg-gray-600 dark:hover:text-white" id="modal-leave" data-modal-toggle="crud-modal">
                    <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
                        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
                    </svg>
                </button>
            </div>

            <!-- Placeholder for form content -->
            <div class="p-4 md:p-5">
                <div class="grid gap-4 mb-4 grid-cols-2">
                    <div class="col-span-2">
                        {{ form_start(form) }}
                        <div class="errors">
                            {{ form_errors(form) }}
                        </div>
                        <div class="space-y-4">
                            <div class="mb-4">
                                {{ form_label(form.reference, null, {
                                    'label_attr': {
                                        'class': 'block text-sm font-semibold text-gray-700 dark:text-gray-300 mb-1'
                                    }
                                }) }}
                                {{ form_widget(form.reference, {
                                    'attr': {
                                        'class': 'w-full p-2 border border-gray-300 rounded-lg text-gray-900 bg-gray-50 dark:bg-gray-700 dark:text-white dark:border-gray-600',
                                        'placeholder': '0 - 999'
                                    }
                                }) }}
                            </div>
                        </div>
                        <div class="space-y-1">
                            {{ form_label(form.roomEntity, null, {
                                'label_attr': {
                                    'class': 'block text-sm font-semibold text-gray-700 dark:text-gray-300'
                                }
                            }) }}
                            {{ form_widget(form.roomEntity, {
                                'attr': {
                                    'class': 'w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm text-gray-900 bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-white dark:border-gray-600 dark:focus:ring-blue-500 dark:focus:border-blue-500'
                                }
                            }) }}
                        </div>

                        <div>
                            <button type="submit" class="mt-5 text-black color6 hover:bg-slate-500 focus:ring-4 font-medium rounded-lg text-sm w-44 px-5 py-2.5 me-2 mb-2 focus:outline-none">
                                Valider
                            </button>
                        </div>

                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
